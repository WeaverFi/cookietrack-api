{
  "name": "cookietrack-api",
  "description": "The API that powers CookieTrack.",
  "version": "2.8.3",
  "scripts": {
    "build": "cd functions && npm run build",
    "start": "npm run build && node functions/dist/index.js",
    "emulator": "npm run build && firebase emulators:start",
    "update": "npm run build && node functions/dist/updates.js",
    "deploy": "npm run update quick && firebase deploy"
  },
  "dependencies": {}
}
